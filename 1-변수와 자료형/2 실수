----------------------------------------------------------------------------------------------------------------------------------------
<배운점>

-고정소수점 방식 과 부동소수점 방식
고정 소수점 방식이란 32bit 또는 64bit속의 비트를 정수부와 소수부로 나누어 자리수를 딱 정해두는 방식이다
표현범위가 크지않다는 것이 단점
부동소수점 방식이란 가수부와 지수부로 나뉜다. 위 코드에서 3e5에서 3은 가수부 5는 지수부이다
표현범위가 훨씬 크다

-e?
실수에서 e는 지수표기법으로 할때 쓰인다.
123.45(소수점표기법) -> 1.2345e2
0.000023 -> 2.3e-5

-float 와 double
float는 4byte double은 8byte로
출력시에 %f 와 %lf로 나타낸다.

-실수의 컴퓨터속 저장형태
소수(7,625)->이진수(111.101)
정규화(1.xxxx * 2^n) 111.101 => 1.11101 *2^2
(부호비트)(지수부)(가수부) 순서로 넣으며된다. 
단, 지수부에는 32bit운영체제 기준으로 127을 더한후 넣어줘야한다. 또한 가수부의 젤 앞 1은 hidden bit로 생략한다.
(1)(1000001)(11101 0000.....0)

----------------------------------------------------------------------------------------------------------------------------------------
<코드>
#include <stdio.h>
#include<math.h>

main() {
	 a, b, c;
	a = 3e5;
	b = 1496e5;
	c = b / (a*60);
	printf("빛의속도는 %fkm/s\n", a);
	printf("태양과 지구와의 거리 %fkm\n", b);
	printf("도달 시간 은 %f분\n", c);
}
----------------------------------------------------------------------------------------------------------------------------------------
