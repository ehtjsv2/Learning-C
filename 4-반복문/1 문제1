--------------------------------------------------------------------------------------------------------------------------------------------
<배운점>
-난수생성
0-잡다한것에 추가하였다.(https://github.com/ehtjsv2/Learning-C/edit/main)
--------------------------------------------------------------------------------------------------------------------------------------------
#include <stdio.h>
#include<stdlib.h>
#include<time.h>

main() {
	int random1,random2;
	int answer;
	srand(time(NULL));
	random1 = rand() % 100; //난수 생성
	random2 = rand() % 100; //난수 생성
	printf("산수 문제를 자동으로 출력합니다\n");
	printf("%d + %d = ", random1, random2);
	scanf(" %d", &answer);
	while (random1 + random2 != answer) { // 틀릴경우
		printf("틀렸습니다.\n");
		random1 = rand() % 100;
		random2 = rand() % 100;
		printf("%d + %d = ", random1, random2);
		scanf(" %d", &answer);
	}
	printf("맞았습니다!\n");
}

--------------------------------------------------------------------------------------------------------------------------------------------
